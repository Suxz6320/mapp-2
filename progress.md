# 进度报告2（第4周·Dart语言基础一）

姓名：李桐  学号：20251060003

---

## 一、任务理解

这次作业主要围绕Dart语言的基础语法展开，分三大块：

1. **案例复现**：跟着指南把dart_basics这个项目搭起来，包含变量类型、函数、控制流三组示例代码，要求能跑、代码跟指南一致、每步有git提交。
2. **自主实践**：5个小任务——把有空安全隐患的代码改写成安全版本、给"实验报告生成器"设计命名参数函数、扩展成绩分级器处理边界和非法输入、用TraeCode做一组对拍练习、标注AI使用情况。
3. **独立研究（选做）**：3个实验——const和final到底差在哪、类型提升规则验证、dart format格式化前后对比。

验收标准：dart run全部输出正确，空安全无编译告警，git有3次以上提交，进度报告按十节模板填写完整。

---

## 二、环境与工具

- 操作系统：Windows 11
- Dart SDK：3.13.3 (stable)
- 编辑器：TraeCode
- AI工具：TraeCode内置AI（用于对拍出题和答疑）
- 运行目标：控制台程序，不涉及Flutter UI
- Git：本地提交推送到GitHub仓库 mapp-2

---

## 三、过程记录

**步骤一：创建工程**
用`dart create dart_basics`生成了项目骨架，看了下bin/dart_basics.dart默认入口，就是个Hello World。然后按指南要求改写。

**步骤二：写四组示例**
在bin目录下建了三个文件：
- types_demo.dart：变量声明、字符串插值、空安全四件套（?.、!、??、late）
- func_demo.dart：普通函数、箭头函数、位置可选参数、命名参数（enroll函数）
- flow_demo.dart：成绩分级gradeOf、for-in遍历、switch、while倒计时

main文件import三个demo，依次调用。

第一次`dart run`的时候发现有个文件里写了`if(name)`想当布尔判断，编译直接报错。想起来了指南里说的"翻车警告"——Dart不支持truthy隐式转换，改成`if(name.isEmpty)`才通过。

**步骤三：对拍练习**
让TraeCode出了5道预测输出题，范围指定空安全、命名参数、整除。我先手写答案再对AI的，5道里4道一致，第4题对`?.`和`??`的优先级有点搞混，查了文档才理清。

**步骤四：自主实践**
- 空安全改写：写了一份有3处隐患的代码（乱用!、不判空就拼字符串、可空传给非空函数），改写后用?.和??替代
- 命名参数：给"实验报告生成器"设计了7个参数的签名，3个required、4个可选带默认值，写了3种调用
- 成绩分级器扩展：在基础版上加了边界判断（0分单独处理、100分正常分级）和非法输入检测（负数、超100、非整数、非数字字符串）

**步骤五：独立研究**
- const vs final：写了Point类做const构造函数实验，const实例会canonicalize（相同值的const实例是同一个），final不会。试了`const x = DateTime.now()`确实编译报错
- 类型提升：验证了if判空后String?会自动提升为String，不用写!。还试了在if块内重新赋null的情况，编译器会正确拒绝后续的非空使用
- dart format：故意写了份缩进乱、空格乱的代码，format后主要改动是统一2空格缩进、运算符两侧加空格、逗号后加空格、单行块展开为多行

**步骤六：git提交推送**
分3次提交：工程创建、demo代码、实践与研究代码。

---

## 四、关键代码

### 4.1 空安全改写前后对照

**改写前（有隐患）：**
```dart
// 隐患1：email是null时直接!会崩
print('邮箱长度: ${email!.length}');

// 隐患2：phone可能为null，拼接出来不好看
print('联系方式: $phone');
```

**改写后（安全版本）：**
```dart
// 用?.安全调用，null时返回null而不是抛异常
print('邮箱长度: ${email?.length}');

// 用??给默认值
print('联系方式: ${phone ?? '未填写'}');
```

说明：这段代码是自己写的，参考了指南里空安全四件套的用法。验证方式是先跑unsafe版本确认会抛`Null check operator used on a null value`，再跑safe版本确认正常输出。

### 4.2 命名参数设计

```dart
String generateReport({
  required String studentName,   // 必填
  required String studentId,     // 必填
  required String experimentNo,  // 必填
  String course = '移动应用开发', // 可选，有默认值
  int? score,                    // 可选，无默认值（可空）
  String format = 'markdown',    // 可选，有默认值
  bool includeCode = false,      // 可选，有默认值
}) { ... }
```

说明：3个required参数是必填的，其余4个可缺省。score用int?而不是给默认值，因为不是每次都有成绩。这段是自主设计的，跑过3种调用方式验证都能正确输出。

### 4.3 成绩分级器扩展

```dart
String classifyGrade(int score) {
  if (score < 0 || score > 100) {
    return '非法输入：分数必须在0~100之间';
  }
  if (score >= 90) return '优';
  if (score >= 80) return '良';
  if (score >= 60) return '中';
  if (score > 0) return '不及格';
  return '零分'; // score == 0
}
```

说明：先做范围校验再分级。0分单独返回"零分"而不是"不及格"，因为0分可能是缺考。非AI生成，自己想的需求。还写了字符串解析版处理"abc"、"95.5"等非法输入。

---

## 五、检查点结果

**dart run输出：** 全部输出正确，无编译告警。

关键输出片段：
```
========== 类型与变量演示 ==========
title = 第一次作业 (类型: String)
---------- 空安全 ----------
nickname?.length = null
nickname ?? 未填写 = 未填写
nickname!.length = 2
...
========== 控制流演示 ==========
7 / 2 = 3.5
7 ~/ 2 = 3
...
========== 全部演示完成 ==========
```

**空安全检查：** `dart analyze`无告警。所有可空变量都通过?.或??或判空后使用，没有裸用!的情况。

**口头解释记录：** `??`是空合并运算符，左边为null取右边；`!`是空断言，告诉编译器"我确定这不是null"，但如果真的是null会抛运行时错误。简单说??是优雅降级，!是赌它非空。

---

## 六、问题与调试

**问题1：if条件传字符串编译报错**

最开始在types_demo.dart里写了`if(name)`想判断name是否为空字符串，编译器报错。Dart里if的条件必须是bool类型，字符串不能当布尔用。改成`if(name.isEmpty)`解决。这个问题指南里有提醒，但还是踩了一次。

**问题2：对拍第4题优先级混淆**

对拍第4题`nums?.length ?? 0`，我一开始以为运算顺序是先`length ?? 0`再`?.`调用，实际是先执行`nums?.length`（安全调用返回null或length值），再对结果做`?? 0`。`?.`优先级高于`??`。查了Dart语言文档确认。

**问题3：dart format --output参数**

想用`dart format --output file.dart`输出到指定文件，结果报错。看了help才知道`--output`只接受write/show/json/none这几种值，不是文件路径。改成先复制文件再format。

---

## 七、AI使用记录

| 用途 | 指令摘要 | 输出 | 验证方式 |
|------|---------|------|---------|
| 对拍出题 | "基于Dart空安全、命名参数、整除知识点出5道预测输出题" | 5道代码题 | 手写答案后逐题对比，实际运行验证 |
| 对拍答疑 | "解释?.和??的优先级关系" | ?.优先级高于??，先安全调用再空合并 | 查dart.dev语言文档确认 |
| 空安全改写参考 | 看了AI生成的安全改写示例 | 了解了?.和??的典型用法 | 自己写代码实际运行验证 |

说明：AI主要用于对拍环节的出题和知识点答疑。代码编写以自主完成为主，AI提供的示例仅作参考，实际代码由自己编写并运行验证。独立研究部分按要求以手工编程和官方文档检索为主，未使用AI生成结论。

---

## 八、证据截图

> 以下截图需自行补充，此处列出需要的截图清单：

1. **dart run输出截图**：dart_basics项目运行，四组demo全部输出正确
2. **dart analyze截图**：空安全无告警
3. **Git提交记录截图**：git log显示3次以上提交
4. **自主实践运行截图**：三个practice文件分别运行的结果
5. **独立研究运行截图**：const_vs_final和type_promotion的输出
6. **dart format对比截图**：format_before和format_after的对比

---

## 九、自评

| 任务项 | 完成情况 | 说明 |
|--------|---------|------|
| 案例复现-dart_basics | 已完成 | 三组demo+main，dart run输出正确 |
| 案例复现-检查点 | 已完成 | dart run全部通过，dart analyze无告警 |
| 案例复现-Git规范 | 已完成 | 3次提交，message遵循约定式提交 |
| 案例复现-报告记录 | 已完成 | 本报告完整填写 |
| 自主实践1-空安全改写 | 已完成 | 3处隐患改写，有前后对照和解释 |
| 自主实践2-命名参数设计 | 已完成 | 7参数签名，3种调用方式 |
| 自主实践3-成绩分级器扩展 | 已完成 | 边界100/0处理+非法输入检测 |
| 自主实践4-对拍记录 | 已完成 | 5题对拍，记录分歧 |
| 自主实践5-AI使用标注 | 已完成 | 本节第七部分 |
| 独立研究1-const与final | 已完成 | 编译错误记录+Point类对比实验 |
| 独立研究2-类型提升 | 已完成 | 5组实验，结论明确 |
| 独立研究3-dart format | 已完成 | 前后对比+diff记录 |

所有必做任务全部完成，选做的独立研究3项也完成了。代码可运行，git提交规范。

---

## 十、一句话收获与下一步计划

**收获：** Dart的空安全设计比我想的要细致——?.、!、??、late四件套各有适用场景，类型提升能在判空后自动去掉?号，用对了代码既安全又简洁。

**遗留问题：** 闭包中类型提升的边界情况还想再深入看看；const构造函数的canonicalize机制只做了简单验证，复杂场景没试。

**下一步计划：** 预习第3课内容（集合类型List/Map/Set和函数进阶），重点看回调和高阶函数部分，因为指南提到第3课会大量用到函数作为参数。
